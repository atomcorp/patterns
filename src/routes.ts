import {routesType} from './types';

const routes: routesType = {
  basic: {
    path: '/basic',
    name: 'Basic',
  },
  repeated: {
    path: '/repeated',
    name: 'Repeated',
  },
};

export default routes;
