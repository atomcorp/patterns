import {routesType} from './types';

const routes: routesType = {
  basic: {
    path: '/basic',
    name: 'Basic',
  },
  repeated: {
    path: '/repeated',
    name: 'Repeated',
  },
  randomTiles: {
    path: 'random-tiles',
    name: 'Random tiles',
  },
};

export default routes;
